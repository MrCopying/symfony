{% extends 'AppBundle::default_layout.html.twig' %}
{% block body %}
    {#{{ dump(authors) }}#}
    {% for author in authors %}
        {{ author.lastName }} | {{ author.firstName }}<br>
    {% endfor %}
{% endblock %}